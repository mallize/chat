"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _toStringTag = _interopRequireDefault(require("./toStringTag"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @function
 * @since v0.0.3
 * @category lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * isSymbol(Symbol.iterator)
 * // => true
 * isSymbol(Symbol('abc'))
 * // => true
 * isSymbol(Symbol.for('abc'))
 * // => true
 *
 * isSymbol('abc')
 * // => false
 */
const isSymbol = value => {
  const type = typeof value;
  return type == 'symbol' || type == 'object' && value != null && (0, _toStringTag.default)(value) == '[object Symbol]';
};

var _default = isSymbol;
exports.default = _default;
//# sourceMappingURL=isSymbol.js.map