"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _identical = require("../common/identical");

var _curry = _interopRequireDefault(require("../common/curry"));

var _objectHasOwnProperty = _interopRequireDefault(require("../lang/objectHasOwnProperty"));

var _objectKeys = _interopRequireDefault(require("../lang/objectKeys"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Performs equality by iterating through keys on an object and returning false when any key has values which are not strictly equal between the arguments. Returns true when the values of all keys are strictly equal.
 *
 * @function
 * @since v0.0.6
 * @category data
 * @param {Array | String | Function} selector The property path to set or functional selector
 * @param {object} objA The object to compare to B
 * @param {object} objB The object to compare to A
 * @returns {boolean} Whether or not the two objects are shallowly equal
 * @example
 *
 * shallowEquals({ a: 1, b: 2, c: undefined }, { a: 1, b: 2, c: undefined }) //=> true
 * shallowEquals({ a: 1, b: 2, c: 3 }, { a: 1, b: 2 }) //=> false
 */
const shallowEquals = (0, _curry.default)((objA, objB) => {
  if ((0, _identical.baseIdentical)(objA, objB)) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  const keysA = (0, _objectKeys.default)(objA);
  const keysB = (0, _objectKeys.default)(objB);

  if (keysA.length !== keysB.length) {
    return false;
  } // Test for A's keys different from B.


  for (let i = 0; i < keysA.length; i++) {
    if (!(0, _objectHasOwnProperty.default)(objB, keysA[i]) || !(0, _identical.baseIdentical)(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
});
var _default = shallowEquals;
exports.default = _default;
//# sourceMappingURL=shallowEquals.js.map